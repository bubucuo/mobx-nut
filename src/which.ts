import {
  makeAutoObservable,
  makeObservable,
  observable,
  action,
  computed,
} from "mobx";

// import {observer} from "mobx-react-lite";
import {observer} from "mobx-react";

export {
  // mobx
  makeAutoObservable,
  makeObservable,
  observable,
  action,
  computed,

  // mobx-react-lite
  observer,

  // mobx-react
};
